package com.audiocast.database;

/**
 * H2 database connection manager with connection pooling
 * Implements 3-connection limit for concurrent operations
 */
public class DatabaseManager {
    // H2 connection & pooling
}
